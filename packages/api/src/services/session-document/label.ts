/**
 * The reserved `artifacts.props.expectedLabel` that marks THE session document.
 * A dependency-free leaf so the access layer (`sessionDocumentReadableWhere`)
 * can name it without importing the session-document service.
 */
export const SESSION_DOCUMENT_LABEL = "session-document";
