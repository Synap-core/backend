/**
 * Suggestions Router - TEMPORARILY DISABLED
 *
 * This router depends on @synap/domain which has been removed.
 *
 * TODO: Refactor to use suggestionRepository directly from @synap/database
 */

import { router } from "../trpc.js";

export const suggestionsRouter = router({
  // All endpoints temporarily disabled - refactor needed
});
