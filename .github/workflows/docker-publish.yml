name: Publish Docker Images

on:
  # Primary: fires after CI quality checks complete on main/develop
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main, develop]
  # Fallback: direct push trigger (the smoke test still gates broken images)
  push:
    branches: [main, develop]
    tags:
      - "v*"
  workflow_dispatch:

# Branch → image tag mapping:
#   develop  → :dev  (staging ring — no CP notification)
#   main     → :main + :sha-* (production ring — notifies CP)
#   v*       → :vX.Y.Z + :latest (release — notifies CP)

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push-backend-api:
    runs-on: ubuntu-latest
    # Only run if CI passed (workflow_run), or if triggered by tag push / manual dispatch
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'push' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    permissions:
      contents: read
      packages: write
      id-token: write

    outputs:
      resolved_ref: ${{ steps.resolve.outputs.ref }}
      resolved_sha: ${{ steps.resolve.outputs.sha }}

    services:
      postgres:
        image: timescale/timescaledb-ha:pg15
        env:
          POSTGRES_USER: synap
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: synap
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
        ports:
          - 5432:5432

    steps:
      - name: Resolve ref and sha
        id: resolve
        run: |
          if [[ "${{ github.event_name }}" == "workflow_run" ]]; then
            echo "ref=refs/heads/${{ github.event.workflow_run.head_branch }}" >> $GITHUB_OUTPUT
            echo "sha=${{ github.event.workflow_run.head_sha }}" >> $GITHUB_OUTPUT
          else
            echo "ref=${{ github.ref }}" >> $GITHUB_OUTPUT
            echo "sha=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.resolve.outputs.sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Convert repository name to lowercase
        id: repo
        run: |
          REPO_OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          REPO_NAME=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]')
          echo "full=${REPO_OWNER}/${REPO_NAME}" >> $GITHUB_OUTPUT

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.repo.outputs.full }}
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=dev,enable=${{ steps.resolve.outputs.ref == 'refs/heads/develop' }}
            type=raw,value=main,enable=${{ steps.resolve.outputs.ref == 'refs/heads/main' }}
            type=raw,value=latest,enable=${{ startsWith(steps.resolve.outputs.ref, 'refs/tags/v') }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build Backend image (local)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./deploy/Dockerfile
          push: false
          load: true
          tags: synap-backend:smoke-test
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

      - name: Run migrations against smoke-test DB
        run: |
          docker run --rm \
            --network host \
            -e DATABASE_URL=postgresql://synap:testpass@localhost:5432/synap \
            --entrypoint sh \
            synap-backend:smoke-test \
            -c "node node_modules/@synap/database/dist/scripts/migrate.js"

      - name: Smoke test — verify container starts
        run: |
          docker run -d --name smoke-test \
            --network host \
            -e DATABASE_URL=postgresql://synap:testpass@localhost:5432/synap \
            -e NODE_ENV=production \
            -e PORT=4000 \
            -e SKIP_SECRET_VALIDATION=true \
            -e ALLOWED_ORIGINS=http://localhost:3000 \
            -e KRATOS_PUBLIC_URL=http://localhost:9999 \
            -e KRATOS_ADMIN_URL=http://localhost:9999 \
            -e HYDRA_PUBLIC_URL=http://localhost:9999 \
            -e HYDRA_ADMIN_URL=http://localhost:9999 \
            -e ORY_HYDRA_SECRETS_SYSTEM=smoke-test \
            synap-backend:smoke-test

          # Wait up to 30s for health check
          for i in $(seq 1 6); do
            sleep 5
            if curl -sf http://localhost:4000/health; then
              echo "Smoke test passed!"
              docker rm -f smoke-test
              exit 0
            fi
            echo "Attempt $i/6..."
          done

          echo "SMOKE TEST FAILED — container logs:"
          docker logs smoke-test
          docker rm -f smoke-test
          exit 1

      - name: Push Backend image to GHCR
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./deploy/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

  build-and-push-pod-agent:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'push' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    permissions:
      contents: read
      packages: write

    steps:
      - name: Resolve ref and sha
        id: resolve
        run: |
          if [[ "${{ github.event_name }}" == "workflow_run" ]]; then
            echo "ref=refs/heads/${{ github.event.workflow_run.head_branch }}" >> $GITHUB_OUTPUT
            echo "sha=${{ github.event.workflow_run.head_sha }}" >> $GITHUB_OUTPUT
          else
            echo "ref=${{ github.ref }}" >> $GITHUB_OUTPUT
            echo "sha=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.resolve.outputs.sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Convert repository name to lowercase
        id: repo
        run: |
          REPO_OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          REPO_NAME=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]')
          echo "full=${REPO_OWNER}/${REPO_NAME}" >> $GITHUB_OUTPUT
          echo "owner=${REPO_OWNER}" >> $GITHUB_OUTPUT

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/pod-agent
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=dev,enable=${{ steps.resolve.outputs.ref == 'refs/heads/develop' }}
            type=raw,value=main,enable=${{ steps.resolve.outputs.ref == 'refs/heads/main' }}
            type=raw,value=latest,enable=${{ startsWith(steps.resolve.outputs.ref, 'refs/tags/v') }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push pod-agent image
        uses: docker/build-push-action@v5
        with:
          context: ./deploy/pod-agent
          file: ./deploy/pod-agent/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

  build-and-push-realtime:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'push' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    permissions:
      contents: read
      packages: write

    steps:
      - name: Resolve ref and sha
        id: resolve
        run: |
          if [[ "${{ github.event_name }}" == "workflow_run" ]]; then
            echo "ref=refs/heads/${{ github.event.workflow_run.head_branch }}" >> $GITHUB_OUTPUT
            echo "sha=${{ github.event.workflow_run.head_sha }}" >> $GITHUB_OUTPUT
          else
            echo "ref=${{ github.ref }}" >> $GITHUB_OUTPUT
            echo "sha=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.resolve.outputs.sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Convert repository name to lowercase
        id: repo
        run: |
          REPO_OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          REPO_NAME=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]')
          echo "full=${REPO_OWNER}/${REPO_NAME}" >> $GITHUB_OUTPUT
          echo "owner=${REPO_OWNER}" >> $GITHUB_OUTPUT

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/backend-realtime
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=dev,enable=${{ steps.resolve.outputs.ref == 'refs/heads/develop' }}
            type=raw,value=main,enable=${{ steps.resolve.outputs.ref == 'refs/heads/main' }}
            type=raw,value=latest,enable=${{ startsWith(steps.resolve.outputs.ref, 'refs/tags/v') }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push realtime image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./deploy/Dockerfile.realtime
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

  notify-control-plane:
    runs-on: ubuntu-latest
    needs:
      - build-and-push-backend-api
      - build-and-push-pod-agent
      - build-and-push-realtime
    # Only notify CP on main branch and version tags — not on develop
    if: >
      needs.build-and-push-backend-api.outputs.resolved_ref == 'refs/heads/main' ||
      startsWith(needs.build-and-push-backend-api.outputs.resolved_ref, 'refs/tags/v')

    steps:
      - name: Compute image tag
        id: tag
        run: |
          REF="${{ needs.build-and-push-backend-api.outputs.resolved_ref }}"
          SHA="${{ needs.build-and-push-backend-api.outputs.resolved_sha }}"
          if [[ "$REF" == refs/tags/v* ]]; then
            TAG="${REF#refs/tags/}"
          else
            TAG="main-$(echo "$SHA" | cut -c1-7)"
          fi
          echo "tag=${TAG}" >> $GITHUB_OUTPUT

      - name: Notify Control Plane
        run: |
          PAYLOAD=$(jq -n \
            --arg tag "${{ steps.tag.outputs.tag }}" \
            --arg gitSha "${{ needs.build-and-push-backend-api.outputs.resolved_sha }}" \
            --arg gitRef "${{ needs.build-and-push-backend-api.outputs.resolved_ref }}" \
            '{tag: $tag, gitSha: $gitSha, gitRef: $gitRef}')

          SIGNATURE=$(echo -n "$PAYLOAD" | openssl dgst -sha256 -hmac "${{ secrets.CP_WEBHOOK_SECRET }}" | awk '{print $2}')

          curl -sf -X POST \
            "${{ secrets.CONTROL_PLANE_API_URL }}/webhooks/github" \
            -H "Content-Type: application/json" \
            -H "X-Hub-Signature-256: sha256=${SIGNATURE}" \
            -d "$PAYLOAD" \
            --max-time 30 || echo "Warning: Control Plane notification failed (non-fatal)"
