{
    # Global options
    email {$EMAIL}
}

# ─── OpenClaw Dashboard ───────────────────────────────────────────────────────
# Exposes the OpenClaw web UI via a public subdomain.
#
# Set up via CLI:  synap openclaw setup-domain
# This writes:
#   - OPENCLAW_DOMAIN    (e.g. openclaw.my-pod.synap.live)
#   - openclaw_auth.snippet  (imported below — auth mode: cp-oauth or basic)
#
# Auth modes:
#   - cp-oauth (managed pods): forward_auth to Better Auth session cookie
#   - basic    (self-hosted):  basicauth with hashed gateway token
#
# In both modes, requests to OpenClaw are forged as X-Real-IP: 127.0.0.1 so
# OpenClaw's gateway auto-approves them as local, bypassing device pairing.
# ─────────────────────────────────────────────────────────────────────────────
{$OPENCLAW_DOMAIN:disabled.invalid} {
    # Auth (one of: cp-oauth forward_auth, basicauth).
    # File is generated by `synap openclaw setup-domain`.
    # Missing file = block is inert (site returns a default response).
    import /etc/caddy/openclaw_auth.snippet

    # Proxy to OpenClaw. X-Real-IP: 127.0.0.1 tells OpenClaw this is a
    # loopback request so it skips device pairing.
    reverse_proxy openclaw:18789 {
        header_up X-Real-IP 127.0.0.1
        header_up X-Forwarded-For 127.0.0.1
        header_up X-Forwarded-Proto {scheme}
    }

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
        Referrer-Policy "strict-origin-when-cross-origin"
    }

    log {
        output file /var/log/caddy/access.log
        format json
    }
}

{$DOMAIN} {
    # Automatic HTTPS with Let's Encrypt

    # Backend API
    handle /trpc/* {
        reverse_proxy backend:4000
    }

    handle /api/* {
        reverse_proxy backend:4000
    }

    # Realtime WebSocket (Socket.IO)
    # Caddy v2 automatically detects and upgrades WebSocket connections
    # No manual header manipulation needed - it breaks the upgrade
    handle /socket.io/* {
        reverse_proxy realtime:4001 {
            # Caddy automatically handles all headers
            # Just preserve X-Real-IP for logging/debugging
            header_up X-Real-IP {remote}
        }
    }
    
    # Realtime HTTP bridge endpoints (for Inngest workers)
    handle /realtime/* {
        reverse_proxy realtime:4001
    }
    
    # Realtime health check (for debugging)
    handle /realtime-health {
        rewrite /realtime-health /bridge/health
        reverse_proxy realtime:4001
    }

    # Ory Kratos (Authentication)
    # Route public API: /.ory/kratos/public/* -> kratos:4433
    # handle_path automatically strips the matched prefix before forwarding
    # Kratos serves internally at /self-service/... (not /public/self-service/...)
    handle_path /.ory/kratos/public/* {
        reverse_proxy kratos:4433
    }
    
    # Route admin API: /.ory/kratos/admin/* -> kratos:4434
    handle_path /.ory/kratos/admin/* {
        reverse_proxy kratos:4434
    }

    # Ory Hydra (OAuth2)
    handle /.ory/hydra/* {
        reverse_proxy hydra:4444
    }

    # MinIO (Object Storage) - Admin UI
    handle /storage/* {
        reverse_proxy minio:9001
    }

    # Pod Agent (CP-initiated commands: update, suspend, restore, configure)
    handle /api/pod-agent/* {
        reverse_proxy pod-agent:4002
    }

    # Health check
    handle /health {
        reverse_proxy backend:4000
    }

    # Admin UI (static SPA + assets under this prefix only — not Kratos
    # `/.ory/kratos/admin`, which is handled above)
    handle /admin* {
        reverse_proxy backend:4000
    }

    # Pod home: redirect exact site root to the console (not a second /admin rule)
    @site_root path /
    handle @site_root {
        redir * /admin/ 302
    }

    # Default: API info (paths that matched nothing above)
    handle {
        respond "Synap Backend API - https://docs.synap.live" 200
    }

    # Security headers
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        X-XSS-Protection "1; mode=block"
        Referrer-Policy "strict-origin-when-cross-origin"
    }

    # Logging
    log {
        output file /var/log/caddy/access.log
        format json
    }
}
