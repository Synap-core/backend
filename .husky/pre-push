#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Types package tsup DTS worker can OOM; give Node 8GB heap for typecheck + build
export NODE_OPTIONS="${NODE_OPTIONS:-} --max-old-space-size=8192"
pnpm typecheck && pnpm build
