# Synap Backend - Local Single-User Configuration

# Database (SQLite for local)
DB_DIALECT=sqlite
SQLITE_DB_PATH=./data/synap.db

# Auth (Simple static token)
SYNAP_SECRET_TOKEN=your-secret-token-here  # Generate with: openssl rand -hex 32

# OpenAI (for AI features)
OPENAI_API_KEY=sk-proj-...

# Inngest (optional for local dev - will use dev server)
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# Environment
NODE_ENV=development
PORT=3000

